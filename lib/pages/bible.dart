import 'package:flutter/material.dart';

class Bible extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Giving Screen Content'),
      ),
    );
  }
}
